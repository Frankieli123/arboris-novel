# MCP 插件系统完整实现规范

## 概述

本规范定义了 MCP 插件系统的完整实现，包括两个核心部分：

1. **MCP 调用功能**：在 LLM 服务中集成 MCP 工具，使 AI 能够调用外部工具来增强生成能力
2. **默认插件管理**：在管理员设置中添加默认插件配置，允许管理员为所有用户配置默认插件

## 当前状态

- ✅ MCP 插件的配置功能已完成（用户可以添加和管理插件）
- ❌ MCP 插件的调用功能未实现（配置了也用不了）
- ❌ 管理员默认插件功能未实现

## 目标

- 让 MCP 插件能够真正被 AI 调用，增强生成能力
- 完善管理功能，支持默认插件配置

## 文档结构

- **requirements.md**: 需求文档，包含 10 个需求和详细的验收标准
- **design.md**: 设计文档，包含架构设计、组件接口、数据模型、正确性属性和测试策略
- **tasks.md**: 任务列表，包含 11 个主要任务和 50+ 个子任务
- **analysis.md**: MuMu 项目 vs 当前项目的对比分析
- **mumu-mcp-usage-details.md**: MuMu 项目中 MCP 的详细使用位置

## 关键特性

### 1. MCP 调用功能

- 在 LLM 服务中实现 `generate_with_mcp()` 方法
- 支持多轮工具调用（最多 3 轮）
- AI 自主决定是否使用工具
- 工具调用失败时优雅降级
- 支持并行执行多个工具调用

### 2. 默认插件管理

- 管理员可以配置默认插件（对所有用户生效）
- 用户可以添加自己的插件（只对自己生效）
- 用户可以启用/禁用默认插件（不影响其他用户）
- 支持插件分类管理
- 支持 JSON 格式的配置和认证请求头

### 3. 数据模型设计

- 使用 `user_id = NULL` 标识默认插件
- 使用 `user_id = 具体ID` 标识用户插件
- 使用 `UserPluginPreference` 表记录用户对默认插件的偏好

## 实现顺序

1. **数据库迁移和模型调整**（任务 1）
2. **Repository 层扩展**（任务 2）
3. **LLM Service MCP 集成**（任务 3，核心功能）
4. **Service 层扩展**（任务 4）
5. **API 层实现**（任务 5）
6. **Frontend 实现**（任务 6）
7. **Schema 和类型定义**（任务 7）
8. **错误处理和日志**（任务 8）
9. **集成测试**（任务 9）
10. **文档和部署**（任务 10）
11. **Checkpoint**（任务 11）

## 测试策略

### 单元测试

- Repository 层测试
- Service 层测试
- API 层测试

### 属性测试（Property-Based Testing）

使用 Hypothesis 库，验证 10 个正确性属性：

1. 默认插件全局唯一性
2. 用户插件用户内唯一性
3. 用户可用插件合并正确性
4. 用户偏好不影响默认插件
5. 工具调用降级一致性
6. 工具列表格式正确性
7. 多轮工具调用终止性
8. JSON 配置验证正确性
9. 插件分类一致性
10. 用户偏好查询正确性

### 集成测试

- 端到端 MCP 生成流程测试
- 管理员配置默认插件流程测试
- 用户启用/禁用插件流程测试

## 参考资料

### MuMu 项目的 MCP 应用场景

1. **大纲生成**：使用 MCP 搜索情节设计参考
2. **章节生成**：使用 MCP 收集章节参考资料
3. **角色生成**：使用 MCP 搜索人物原型参考
4. **向导流式生成**：实时搜索并返回进度

### 统一的调用模式

```python
# 所有场景都遵循这个模式
if enable_mcp:
    mcp_result = await ai_service.generate_with_mcp(
        prompt="搜索XXX参考资料",
        user_id=user_id,
        enable_mcp=True,
        max_tool_rounds=3
    )
    reference_materials = mcp_result["content"]
    final_prompt = f"{base_prompt}\n\n参考资料：\n{reference_materials}"
```

## 开始实现

要开始实现此规范，请：

1. 阅读 `requirements.md` 了解详细需求
2. 阅读 `design.md` 了解技术设计
3. 打开 `tasks.md` 并点击"Start task"开始执行任务

## 注意事项

- 所有测试任务都是必需的（已移除可选标记）
- 每个任务都标注了对应的需求编号
- 属性测试使用 Hypothesis 库，需要配置至少 100 次迭代
- 每个属性测试都需要明确标注对应的正确性属性编号
